# T4.1 — Heavyweight trace capture
Phase: 4   Depends-on: T3.4   Status: done (Sudnya signed off 2026-06-17: capture + determinism + read-set green; T4.2 fidelity spot-check PASSED; heavy-trace.md FROZEN)

## Context
Plan Phase 4: instrument the trusted JS machine to record, per subroutine
invocation, the data the port tests need. Only legitimate once T3.4 proves the
machine MAME-faithful on the deterministic window. Schema: cdoc/schemas/heavy-trace.md.

## Entry criteria
- [x] T3.4 done (exact-match gate green on its scoped definition; signed off
      2026-06-16, visible-rows window frames 0-258).
- [x] scenario input scripts exist in traces/scripts/.

## Task
Instrument CALL/RET dispatch + memory taps (taps designed into T2.4). Per
invocation capture: entry PC, regs_in, ORDERED read-set [(addr,val)...],
regs_out, write-set, cycles, caller PC (call graph for free). Per-instruction
mode on demand only. Emit per cdoc/schemas/heavy-trace.md; freeze that schema.

## Out of scope
Annotation (T6). Test-plan generation (T7). Cycle-timing work.

## Definition of done
- [x] Same input script twice → byte-identical heavyweight traces (determinism
      proof) — in npm test (`heavy trace is deterministic`, 81/81 green).
- [x] Call graph emitted (callerPC→entryPC; all 391,352 non-ISR callerPCs verified
      to hold a CALL/RST opcode in the decode oracle); ordered read-set preserves
      repeated live-port reads (`read-set preserves repeated same-port reads`).
- [ ] MAME routine-level fidelity spot-check (3-5 routines in frames 0-258):
      regs_in/read_set/regs_out/write_set match MAME exactly. NOT YET DONE.
- [x] T7/T8 consumption caveats documented in heavy-trace.md §"Consumption
      notes" (5 items: instruction-fetch reads, return-address stack noise,
      magic-RAM post-ALU writes, unmarked nested-call position, coroutine
      mis-nesting) + decisions.md entry (2026-06-17 Cowork review).
- [ ] heavy-trace.md marked FROZEN (only after the spot-check); decisions.md entry.

## Verification
```
cd machine && npm test
```

## Result (2026-06-17, in_progress)
Built `machine/tools/heavy_trace_capture.js` against the real CPU/scheduler API
(no invented event bus): mem/IO captured by wrapping `machine.cpu.callbacks`
(mem vs io tagged; the adapter's opcode-peek suppressed so the read-set is the
core's true access sequence); per-instruction boundary via `scheduler.onStep`;
ISR entry via a wrapped `cpu.interrupt`; entry = taken CALL/RST (SP-2), exit =
SP-depth rise (covers RET/RET cc/RETI/RETN). Schema: cdoc/schemas/heavy-trace.md.
CLI run over attract-only (3085 frames): 416,482 invocations; determinism test
green; correctness cross-check vs decode oracle: 0 bad callerPCs. STILL OWED
before freeze/awaiting-human: the MAME routine-level fidelity spot-check.
