# T8.1 — JS test bench
Phase: 8   Depends-on: T7.1   Status: blocked

## Context
Plan Phase 8: run test plans against JS routine implementations with NO
emulator in the loop. ~200 lines. Mocks memory from the ordered read-set,
calls a JS routine, diffs regs_out + write-set.

## Entry criteria
- [ ] T7.1 done (JSONL test plans + frozen schema).

## Task
machine/tools/bench.js: load a routine's JSONL cases, build a mock memory from
each case's ordered read-set, invoke the JS routine, diff regs_out + writes,
report per-routine pass/fail. Wire into npm test.

## Out of scope
Writing the actual routine ports (T9). Any emulator dependency.

## Definition of done
- [ ] Bench runs a sample routine's cases and reports pass/fail; exit non-zero
      on mismatch.
- [ ] Hermetic (no CPU core / no full machine imported).
- [ ] Wired into npm test.

## Verification
```
cd machine && npm test
```
