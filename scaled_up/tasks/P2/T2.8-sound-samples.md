# T2.8 — Sound/speech via samples
Phase: 2   Depends-on: T2.4   Status: awaiting-human

## Context
Decision (see cdoc/decisions.md + plan Phase 2): do NOT emulate the S14001A
speech chip or discrete sound circuits initially. Trigger recorded samples
keyed on the CPU's writes to the sound/speech ports. CPU-visible contract:
`cdoc/hardware-berzerk.md` §7. At the project's fidelity bar (feel), samples
are indistinguishable; chip emulation is a later opt-in if that proves wrong.

## Entry criteria
- [ ] T2.4 done. Spec §7 lists the write→utterance/effect mapping.
- [ ] HUMAN-GATE: sample audio files supplied by human into
      `machine/fixtures/samples/` (user-sourced; never committed).

## Task
`machine/src/sound.js`: port-write handler decoding speech commands and sound
triggers per spec → emits named events (`speech:intruder_alert`,
`sfx:laser`, ...). Audio backend is pluggable: a silent/recording backend for
tests (logs event sequence), WebAudio backend for the browser (T2.9 wires it).
Busy/ready status reads (if spec documents them) return plausible values —
note any timing approximation in cdoc/decisions.md.

## Out of scope
S14001A internals. Audio mixing quality. Browser autoplay policy (T2.9).

## Scope note (2026-06-12): CODE-only; audio split to T2.8b
Sudnya's decision: samples are cosmetic (golden frames hash video, not audio)
and the capture is a rabbit hole, so T2.8 = the decoder CODE only. The audio
files + populated manifest + the SFX-burst key design moved to T2.8b (deferred,
non-blocking). T2.9 no longer depends on audio files (boots silent /
sample-optional). See cdoc/decisions.md.

## Definition of done
- [x] Unit tests: documented port-write sequences produce the expected event
      sequence (cases cite §7). -- speech word load, control (vol/clock), sfx,
      sfxctrl, ordered sequence.
- [x] Status-read behavior tested. -- §7 doesn't pin the read; readStatus()
      returns ready (documented timing approximation in decisions.md).
- [x] `npm test` passes (silent backend; no audio files needed). -- 52/52.
- [moved] HUMAN-GATE: sample audio files + manifest -> T2.8b (deferred).

## Verification
```
cd machine && npm test
```

## Result
Status: awaiting-human (CODE complete -> ready for flip to done; audio deferred
to T2.8b). machine/src/sound.js + machine/tests/sound.test.js (8 tests) green.
Behavior pinned in hardware-berzerk.md §7 (citations); decisions/approximations
+ the partial-capture provenance and model correction in cdoc/decisions.md
(2026-06-12). The audio HUMAN-GATE (files, identities, manifest rows, SFX-burst
key design) is now T2.8b, which blocks nothing.

sound.js decodes audio-port writes per §7 to a pluggable backend
(RecordingBackend for tests/headless -- no files; WebAudio at T2.9). Events are
spec-pure: speech keyed by S14001A word address, raw sfx/sfxctrl by port/data
(semantic names are NOT pinned by §7 -- see naming note below). readStatus()
reports ready (documented approximation: sample playback is fire-and-forget).

`cd machine && npm test` (exit 0):
```
✔ Section 7: writing 0x44 with mode 0 plays the 6-bit word address
✔ Section 7: 0x44 mode 1 sets volume and clock divisor per the formulas
✔ Section 7: 0x44 modes 2 and 3 are unused (no event)
✔ Section 7: 0x46 emits sfxctrl; 0x40-0x43/0x45/0x47 emit raw sfx writes
✔ writes outside 0x40-0x47 are ignored
✔ Section 7: a control-then-speak-then-sfx sequence yields ordered events
✔ S14001A status read reports ready (bit 6 set)
✔ default SilentBackend requires no audio files and is inert
ℹ tests 52
ℹ pass 52
ℹ fail 0
```
(52 = 8 sound + 9 input + 8 scheduler + 10 video + 16 memory + 1 smoke; green.)

### Audio -> moved to T2.8b (deferred, non-blocking)
Auditioning/identifying clips, supplying .wav files, populating the manifest,
and resolving the SFX-burst key design are all in T2.8b now. Nothing depends on
them. The partial capture (MAME 0.288; 4-word speech phrase 0x10/0x09/0x0b/0x11;
one SFX burst fired 8x; no .wav files) is recorded in decisions.md and T2.8b.

### Naming decision: RESOLVED -> option (a) (Sudnya, 2026-06-12)
sound.js stays address-keyed and spec-pure (no vocabulary table in the decoder).
Event->file mapping lives in machine/fixtures/samples/manifest.json (committed;
format in that dir's README): speech keys = word address as "0x"+hex -> filename;
sfx TBD. The T2.9 WebAudio backend loads it. Created the manifest skeleton +
.gitignore exception + README format section.

Provenance is PENDING from Sudnya: a MAME Lua hook on the 0x44 speech-write logs
the word address per phrase; after audition Sudnya supplies `mame -version` and
the addr->identity->filename table, which I then write into decisions.md and
manifest.json. T2.8 remains awaiting-human until the .wav files + populated
manifest are in place.
