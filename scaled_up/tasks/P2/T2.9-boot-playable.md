# T2.9 — Boot to playable game (browser shell)
Phase: 2   Depends-on: T2.3, T2.4, T2.5, T2.6, T2.7, T2.8   Status: awaiting-human

## Context
Integration milestone: all components assembled into a machine that boots the
real ROM and plays. This is the Phase 2 exit gate. Expect to find cross-
component bugs here — that's its job.

## Entry criteria
- [x] T2.3–T2.8 all done (`cd machine && npm test` green; ZEX gate green at T2.3).
- [x] Berzerk RC31 ROM set present at repo-root rom/berzerk/ (user-supplied).

## Task
1. `machine/src/machine.js`: compose CPU+memory+video+scheduler+input+sound;
   `reset()`, `runFrame()`, `loadRoms(paths)`.
2. `machine/shell/index.html` + minimal JS: canvas blit of `renderToRGBA`,
   requestAnimationFrame pacing at the original frame rate (scheduler stays
   wall-clock-free; the shell paces it), keyboard → `setField` mapping,
   WebAudio backend hookup. Serve via `npx http-server` or equivalent —
   no build step.
3. Headless smoke test: boot ROM, run N frames, assert VRAM is non-trivial
   (attract mode drew something) and CPU hasn't halted/wedged.

## Out of scope
Golden-frame validation vs MAME (Phase 3 — playability here is necessary,
not sufficient). Trace capture (Phase 4). Performance beyond "hits frame rate
on a laptop".

## Definition of done
- [x] Headless smoke test passes in `npm test` (skips gracefully if ROM path
      not configured, with clear message). -- boots the real ROM; asserts VRAM
      drawn + PC in game ROM + not halted.
- [ ] HUMAN-GATE: human plays in browser — boots to attract mode, coin/start
      works, full game loop (move, shoot, die, next maze) playable, speech
      samples fire at sensible moments. (Audio is deferred -> T2.8b; "samples
      fire" is best-effort/optional here.)
- [ ] Defects found are filed as notes below, fixed or accepted.

## Verification
```
cd machine && npm test        # plus the HUMAN-GATE play session
# browser: cd machine && npx http-server, open shell/index.html, pick rom/berzerk files
```

## Result
Status: awaiting-human (HUMAN-GATE: browser play; per rule #5). The
machine-checkable half is done and green.

Built: machine/src/machine.js (composition + I/O dispatch + loadRoms + reset +
runFrame), machine/src/roms.js (RC31 layout/assembly), machine/shell/ (no-build
canvas page: wall-clock-paced rAF at 59.64 Hz with a wall-clock-free scheduler,
keyboard -> setField, sample-optional WebAudio), machine/tests/machine.test.js.
Wiring + boot result + traps in cdoc/decisions.md (2026-06-12).

`cd machine && npm test` (exit 0):
```
✔ machine composes and runs frames without ROMs (no throw)
✔ boots the real ROM: VRAM drawn, CPU not wedged
ℹ tests 54
ℹ pass 54
ℹ fail 0
```
Boot trace (600 frames on the real RC31 ROM): self-test runs (VRAM fully filled
~frame 240), then game code -- frame 600 pc=0x1517 (game ROM), sp in work RAM,
VRAM ~520 non-zero, color RAM 1792/2048. The Magic RAM self-test (PC 0x0458 hang
in the original when the 74181 was wrong) PASSES. No cross-component bug surfaced
headlessly.

### HUMAN-GATE — how to play
1. `cd machine && npx http-server` (or any static server), open
   `http://localhost:8080/shell/index.html`.
2. Pick the 6 `rom/berzerk/` program files in the file input (rom0 + rom1-5).
3. Click the page; controls: arrows move, Space/Z fire, 1/2 start, 5 coin, P pause.
4. Confirm: attract mode visible, coin+start enters a game, move/shoot/die/next-maze
   all work. (No audio yet -- deferred to T2.8b; manifest is empty/silent.)

### Defect notes (play session)
- (none yet -- to be filled from the play session; headless boot was clean.)
- Known fronts a play session may surface (all flagged earlier, not bugs per se):
  pixel bit-order and RGBI pen levels are display approximations to confirm
  visually (§4.5); speed/feel rides on the 59.64 Hz pacing.
