# T2.8b — Capture & map audio samples (deferred; non-blocking)
Phase: 2   Depends-on: T2.8   Status: pending

## Context
Split out of T2.8 (2026-06-12, Sudnya). T2.8's CODE is done -- sound.js decodes
the audio-port writes and emits events; the default silent backend needs no
files. The actual sample audio + the populated manifest are cosmetic (the
golden-frame gate hashes VRAM+color, NOT audio) and the capture proved a rabbit
hole, so they are deferred here and block NOTHING. T2.9 boots with the
silent / sample-optional backend; the WebAudio backend plays whatever manifest
rows exist (zero is fine).

## Entry criteria
- [ ] T2.8 done (decoder + events + manifest format).

## Task
Capture Berzerk audio in MAME, audition, and populate
`machine/fixtures/samples/` (user-supplied .wav, never committed) +
`manifest.json`. Tooling already exists: `machine/tools/mame/speechlog.lua`
taps io 0x40-0x47 and logs SPEECH word addresses + SFX writes.

## State from the partial capture (2026-06-12; see cdoc/decisions.md)
- MAME 0.288. Log: `machine/speechlog.txt` (gitignored; regenerable).
- SPEECH: one phrase = 4 word addresses (0x10, 0x09, 0x0b, 0x11), unidentified.
- SFX: ONE distinct triggered effect (multi-write burst
  {0x40=0x82, 0x42=0x01, 0x43=0x01, 0x46=0x47}) fired 8x -- NOT 8 distinct
  effects. Plus continuous ambient tone (high-count writes) that sample
  triggering does not reproduce (samples-approach limitation).
- No .wav files captured into the dir yet.

## Open design (left for this task)
- SFX manifest key: an SFX is a multi-write BURST, so the speech-style
  address key does not apply. Decide a burst-signature key + how the T2.9
  WebAudio backend DETECTS a burst from the live write stream. The samples
  README documents the speech key format; the SFX key is still "TBD".
- Speech phrase vs word: the one clip is 4 word-events; decide whether the
  manifest maps per-word (0x10/0x09/0x0b/0x11 -> word samples) or per-phrase.
- Whether the 8 identical SFX bursts collapse to a single sample.

## Definition of done
- [ ] HUMAN-GATE: .wav files in machine/fixtures/samples/ (never committed),
      speech words identified, SFX effect(s) identified.
- [ ] manifest.json populated (speech + sfx keys) per the resolved formats.
- [ ] Provenance (MAME version, in-game action per clip) in cdoc/decisions.md.

## Verification
```
cd machine && npm test    # stays green; manifest rows are optional
```
