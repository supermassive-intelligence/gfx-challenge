# T2.3 — Z80 core: vendor + validate (ZEX + SingleStepTests gates)
Phase: 2   Depends-on: T2.1   Status: in-progress

## Incident record (do not delete)
- 2026-06-10: this task was marked `done` with a hand-translated core that
  fails immediately. Human verification (Sudnya) ran the command and got:
  `Error: Unknown opcode 0xf9 at PC 0x0116` (LD SP,HL — base opcode) on BOTH
  zexdoc.com and zexall.com. Only ~26 of 256 base opcodes existed. The
  decisions.md claim "passes ZEXALL" referred to the C original, not this
  port. Status reverted; task respecified below. Lesson: verification output
  must be PASTED into the Result section, never summarized.

## Context
We do NOT hand-write or hand-translate a Z80 core — a de-novo translation is
writing a core (see cdoc/decisions.md). The core must pass the standard
exercisers before any trace from it is trusted. Per-opcode JSON tests give
pinpoint failures; zex gives only end-of-group CRCs — use both.

## Entry criteria
- [ ] T2.1 done (`cd machine && npm test` passes).
- [ ] `zexdoc.com`/`zexall.com` in `machine/fixtures/` (provenance noted).

## Task
1. Delete the hand-translated `machine/src/cpu/z80.js` opcode implementation.
2. Vendor a real core, preferred order:
   a. DrGoldfire/Z80.js as-is (MIT). Known deviation: undocumented X/Y flag
      behavior in BIT n,(HL) — characterize via step 4 and record in
      decisions.md (1980s arcade code cannot plausibly depend on it).
   b. If (a) proves unsuitable: superzazu/z80 C compiled to WASM via emcc,
      vendored as a binary artifact (mechanical transform, logic preserved).
3. Keep the thin swappable interface in `machine/src/cpu/` (memory/IO
   callbacks, per-instruction step, cycle counts, IRQ/NMI lines).
4. Build `machine/tools/run_sst.js`: runner for the SingleStepTests/z80 JSON
   suite (github.com/SingleStepTests/z80) — per-opcode, ~1000 cases each.
   Register-and-memory comparison mode is sufficient (cycle/bus-level
   comparison out of scope). Record pass/fail counts per opcode; documented
   known-deviation opcodes may be allowlisted ONLY with a decisions.md entry.
5. Keep `machine/tools/run_zex.js` as the integration gate.

## Out of scope
Hand-writing/translating opcode logic. Cycle/bus-level SST comparison.
Berzerk devices. Performance.

## Definition of done
- [ ] `node tools/run_sst.js` exits 0 (all opcodes pass, or deviations
      allowlisted with decisions.md entry). PASTE summary output below.
- [ ] `node tools/run_zex.js fixtures/zexdoc.com` runs to completion, all
      tests OK, exit 0. PASTE final output below.
- [ ] zexall.com run completes; result + any deviations documented. PASTE.
- [ ] Core provenance (repo, commit, license) in decisions.md; prior
      incorrect entry corrected there.
- [ ] HUMAN-GATE: Sudnya re-runs both verification commands personally and
      flips this task to done.

## Verification
```
cd machine && npm test && node tools/run_sst.js && node tools/run_zex.js fixtures/zexdoc.com
```

## Result
(pending — paste actual command output here, not summaries)
