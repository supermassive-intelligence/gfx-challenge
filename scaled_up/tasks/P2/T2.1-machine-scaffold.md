# T2.1 — machine/ project scaffold + test runner
Phase: 2   Depends-on: none   Status: done

## Context
The JS machine lives in `machine/`. Conventions per `CLAUDE.md`: vanilla ES
modules, Node ≥ 20, built-in `node:test`, no build step.

## Entry criteria
- [ ] Node ≥ 20 available (`node --version`).

## Task
Create `machine/` with: `package.json` (`"type":"module"`, `npm test` →
`node --test tests/`), directories `src/ tests/ tools/ fixtures/`, a trivial
passing smoke test, `machine/README.md` (one paragraph + pointer to
`cdoc/z80-port-plan.md`), and `.gitignore` entries for ROMs and large traces.

## Out of scope
Any emulation code. Browser shell. Dependencies beyond a Z80 core placeholder
note (core chosen in T2.3, not here).

## Definition of done
- [ ] `cd machine && npm test` exits 0 running ≥ 1 test via node:test.
- [ ] Directory layout matches CLAUDE.md.

## Verification
```
cd machine && npm test
```
