# T2.7 — Input ports + DIP switches
Phase: 2   Depends-on: T2.4   Status: awaiting-human

## Context
CPU-visible input: joystick/buttons/coin/start ports and DIP switch banks.
Spec: `cdoc/hardware-berzerk.md` §3 (IO map) and §6 (DIPs/field names).
Field names must match the MAME driver exactly — the input-script schema
(`cdoc/schemas/input-script.md`) resolves names on both platforms.

## Entry criteria
- [x] T2.4 done (`npm test` passes).

## Unblocked — §6 pinned from source (was blocked on spec gap)
Sudnya pasted INPUT_PORTS_START(berzerk) + BERZERK_COINAGE. §6 now pins the
read-port map, per-bit fields/polarity, DIP defaults, the coinage table, and the
three traps (mixed-polarity SW2, fake MONITOR_TYPE, base-set defaults). See
cdoc/decisions.md (2026-06-12).

## Task
`machine/src/input.js`: port state objects keyed by the documented
port/field names; reads return correct bit packing (active-low where spec
says so); DIP banks configurable at construction, defaulting to the spec's
factory settings; programmatic `setField(port, field, value)` API — this is
what both the keyboard shell (T2.9) and the input-script player (T3.1) call.

## Out of scope
Keyboard mapping/browser events (T2.9). Script replay (T3.1).

## Definition of done
- [x] Unit tests: every documented field flips exactly its documented bit;
      polarity verified (incl. SW2 mixed active-high/low); DIP defaults match §6.
- [x] Unknown port/field names throw (typo guard) -- also unknown settings and
      out-of-mask DIP values.
- [x] `npm test` passes. -- 44/44.

## Verification
```
cd machine && npm test
```

## Result
Status: awaiting-human (per standing rule #5; final review/flip is Sudnya's).
Implemented machine/src/input.js + machine/tests/input.test.js (9 tests), all
green. Behavior pinned in hardware-berzerk.md §6 (citations); design/traps in
cdoc/decisions.md (2026-06-12).

input.js: per-bit-polarity port composition keyed by canonical field names;
DIPs default to the factory (base `berzerk`) settings, configurable at
construction or via setField (by setting name or masked value); setField throws
on unknown port/field/setting and out-of-mask values; readPort(name) and
readAddress(addr) (with the 0x18 DIP mirror). MONITOR_TYPE deliberately excluded
(fake config port -- T2.5 video, not CPU-readable).

`cd machine && npm test` (exit 0):
```
✔ Section 6.2/6.3: factory default port reads
✔ Section 6.2: each input field flips exactly its bit at its polarity
✔ Section 6.2: SW2 mixed polarity (active-high services over active-low unused)
✔ Section 6.3: DIP factory defaults match the spec
✔ Section 6.3: DIPs settable by name and by masked value; Cabinet to Cocktail
✔ constructor DIP overrides apply (validated)
✔ Section 6.4: coinage table covers all 16 nibble values with no gaps
✔ Section 6.1: readAddress maps ports and honors the 0x18 DIP mirror
✔ typo guard: unknown port/field/setting and out-of-mask values throw
ℹ tests 44
ℹ pass 44
ℹ fail 0
```
(44 = 9 input + 8 scheduler + 10 video + 16 memory + 1 smoke; full suite green.)

### For the human review
- The three traps from your source notes are all handled + tested: SW2 packs
  per-bit polarity (SERVICE1/2 active-high vs unused active-low); MONITOR_TYPE
  is absent from the port map (video config only); F3 Language default 0x00 is
  the base-`berzerk` (English) value, recorded as set-specific in decisions.md.
- Field names are the canonical schema names; the setField typo-guard is the
  hook T3.1/T3.2 cross-platform replay relies on.
- Out of scope / deferred: keyboard/browser mapping (T2.9), script replay (T3.1).
  Wiring readAddress into the I/O dispatch is T2.9.
