# T9.2 — Port routines bottom-up (iterative)
Phase: 9   Depends-on: T9.1   Status: blocked

## Context
Plan Phase 9 main body: replace Z80 routines with JS one at a time, leaves of
the call graph first, each verified before the next. This is an iterative,
long-running task — track progress as a checklist of routines.

## Entry criteria
- [ ] T9.1 done (hook harness + one routine proven).

## Task
Per routine (leaves first): implement in JS by intent (delay loops → explicit
waits, not cycle-burning); pass all its T8 bench cases; register the CALL hook;
re-run the full scoped golden-frame suite. Commit one routine per change with
the routine name in the message. Maintain a coverage checklist (ported / total).

## Out of scope
Removing the Z80 core / re-anchoring the loop (T10).

## Definition of done
- [ ] 100% of trace-reachable routines ported and hooked.
- [ ] Bench green for all; scoped golden-frame suite green at every commit.
- [ ] Coverage checklist shows 100%; HUMAN-GATE: Sudnya confirms playable
      throughout via spot play sessions.

## Verification
```
cd machine && npm test && npm run golden
```
