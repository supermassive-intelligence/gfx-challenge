# T7.1 — Test-plan generator
Phase: 7   Depends-on: T4.1, T6.1   Status: done (Sudnya signed off 2026-06-18: self-check 3/3 reproduced independently; 400 records / 47 routines; RANDOM records LCG-verified 4/4; r-exclusion + IO-port-normalization + leaf-first policies ratified)

## Context
Plan Phase 7: convert heavyweight traces into hermetic per-routine test cases.
One JSONL per trace, one record per kept invocation. Schema:
cdoc/schemas/test-plan.md.

## Entry criteria
- [ ] T4.1 done (traces). T6.1 done (routine names for readable cases).

## Task
Emit per cdoc/schemas/test-plan.md: {routine, entry_pc, regs_in, reads, regs_out,
writes, cycles, path_id}. Dedupe identical invocations; keep N distinct branch
paths per routine. Self-check: replay generated records on the JS machine and
confirm they reproduce.

## Out of scope
The bench that runs them (T8). Porting (T9).

## Definition of done
- [ ] JSONL test plans generated; schema FROZEN.
- [ ] Self-check passes: replaying records on the JS machine reproduces
      regs_out + writes (in npm test).
- [ ] decisions.md entry on selection policy.

## Verification
```
cd machine && npm test
```

## Result (awaiting-human)

Generator: `machine/tools/generate_test_plan.js`. Schema FROZEN:
`cdoc/schemas/test-plan.md`. Self-check: `machine/tests/test-plan.test.js` (in
`npm test`). Plans: `traces/test-plans/*.jsonl` (one per input script).

Approach: validate/consume each record by EXECUTING the routine on a fresh Z80
core against a mock memory (real ROM + writable-region read seeds + ordered IO
FIFO). Selection is self-validating — only records that reproduce regs_out+writes
are emitted; this makes "leaf-first" emergent (non-leaf invocations execute their
callees inline and so fail the callee-excluded write_set check, and are deferred
to T9 bottom-up). See decisions.md 2026-06-18 for the full policy.

Evidence:
```
$ cd machine && npm test
  tests 86  pass 86  fail 0
  (incl. test-plan: emits records / every emitted record self-validates / deterministic)

$ node tools/generate_test_plan.js ../traces/scripts/attract-only.jsonl <out>
  test plan: 109 records over 47/83 routines
  invocations 416482; replayed_ok 325598; failed 90884
    {isr,writes(non-leaf),regs+writes,io-starved,no-return(coroutine),regs}; maxKeptPath 279

# committed plans (all 5 scripts), independent from-disk replay:
  committed plans: 400 records, 400 reproduce, 0 FAIL
```

Coverage (union of 5 scripts): 400 records over 47 distinct routines (= the
attract leaf set; gameplay scripts add path diversity, not new leaves). Strong
path coverage on the hot routines: DRAW_SPRITE 8 paths, COORD_TO_MAZECELL 8,
the actor scheduler 8, MOVE_ANIMATE_VECTOR/SHOOT 6, COLLISION_DETECTION 6.
The other 36 attract-reachable routines are non-leaf/ISR/coroutine — excluded by
the self-check, testable bottom-up as their children port (T9), exactly as
heavy-trace.md note #4 intends.

DoD check: [x] JSONL plans generated + schema FROZEN; [x] self-check passes in
`npm test` (replay reproduces regs_out + writes); [x] decisions.md selection-policy
entry. HUMAN-GATE: Sudnya reviews coverage and ratifies the leaf-first selection +
`r`-exclusion policy before this flips to done. Committed nothing.
