# T5.2 — Full nondeterministic-read-site catalog
Phase: 5   Depends-on: T5.1, T4.1   Status: blocked (needs traces from T4.1)

## Context
Plan Phase 5: enumerate EVERY source of nondeterminism so traces are
reproducible and so port-divergence bugs become a checklist. Builds on the
single confirmed source from T5.1 by sweeping traces for all timing/entropy
reads (frame counters, R register, free-running timers, input-timing seeds).

## Entry criteria
- [ ] T5.1 done (primary entropy source confirmed).
- [ ] T4.1 done (heavyweight traces + call graph exist).

## Task
Sweep traces + disassembly for all nondeterministic read sites; for each:
address, source, what consumes it, whether it affects visible state. Produce
cdoc/entropy-berzerk.md (full table). Note which sites are reproducible within
the JS machine (deterministic there) vs genuinely MAME-divergent.

## Out of scope
Cycle-timing parity work. Changing the core.

## Definition of done
- [ ] cdoc/entropy-berzerk.md lists every nondeterministic read site with
      address + consumer + visible-impact flag.
- [ ] Cross-checked against T5.1's finding (no contradictions).
- [ ] decisions.md entry.

## Verification
```
test -s cdoc/entropy-berzerk.md   # plus human review of completeness
```
