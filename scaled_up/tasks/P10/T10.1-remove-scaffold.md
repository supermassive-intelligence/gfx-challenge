# T10.1 — Remove the scaffold
Phase: 10   Depends-on: T9.2   Status: blocked

## Context
Plan Phase 10: finish the inversion — delete the Z80 interpreter from the
runtime path; the rAF loop + JS interrupt cadence drive the game. The attempt
to remove the core is itself the flush test for 100% coverage.

## Entry criteria
- [ ] T9.2 done (100% routine coverage; every CALL hooked).

## Task
Remove the Z80 core from the runtime path (keep it in-repo for the next game /
debug builds). Re-anchor main loop + interrupt cadence (the T2.6 NMI/IRQ
schedule) to requestAnimationFrame / audio clock at the original frame rate.
Flush test: full run with no interpreter to fall back to.

## Out of scope
New features. Cycle-timing parity (still deferred unless a gate demands it).

## Definition of done
- [ ] Z80 core removed from runtime path; game runs pure-JS.
- [ ] Scoped golden-frame suite still green; long run with no core has zero
      fall-through (flush test passes — any unported routine now errors loudly).
- [ ] HUMAN-GATE: Sudnya side-by-side play vs MAME confirms indistinguishable
      feel.
- [ ] decisions.md: project-complete entry; reusable-pipeline assets listed.

## Verification
```
cd machine && npm test && npm run golden   # plus HUMAN-GATE play session
```
